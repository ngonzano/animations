import 'package:flutter/material.dart';

class AnimatePageRouteTransitionDemo extends StatelessWidget {
  const AnimatePageRouteTransitionDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('modificar'),
    );
  }
}
