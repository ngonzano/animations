import 'package:flutter/material.dart';

class StaggeredMenuAnimation extends StatelessWidget {
  const StaggeredMenuAnimation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('modificar'),
    );
  }
}
