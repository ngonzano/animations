import 'package:flutter/material.dart';

class AnimatePageUsingPhysicsSimulation extends StatelessWidget {
  const AnimatePageUsingPhysicsSimulation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('modificar'),
    );
  }
}
